#pragma once
#include "common/vector.h"

double minmax_angle_ratio(Vec2 const& av, Vec2 const& bv, Vec2 const& cv);
double minmax_length_ratio(Vec2 const& av, Vec2 const& bv, Vec2 const& cv);
double inscribedcircum_ratio(Vec2 const& av, Vec2 const& bv, Vec2 const& cv);